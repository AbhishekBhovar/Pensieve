Pensieve 1.7

Critical rebuild:
- Rebuilt navigation without JavaScript.
- Every app control is now a real HTML link, so Safari/GitHub Pages can navigate even if JavaScript is unavailable.
- No invisible JavaScript overlay can block buttons.
- Loading is CSS-only: 4 seconds, 0% on the left, 100% on the right, and a bar that visibly fills between them.
- No stacked percentage text.
- Replaced ugly text-symbol bottom nav icons with consistent custom SVG outline icons.
- Full static reflection flow retained for “I’ve failed”.
- Other five emotional starter flows included.
- Locked artwork/logo remain unchanged.
